# Each node includes one and only one toplevel::xxx class, as well as some
# node-specific variables.
#
# This system replaces node inheritance, and is forward-compatible to use of an
# ENC, which cannot represent node inheritance.
