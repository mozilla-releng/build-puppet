MAILTO="<%= scope.lookupvar('::config::puppet_notif_email') %>"
<%= minute1 %>,<%= minute2 %> * * * * root /usr/bin/puppet agent --enable && /usr/bin/puppet agent --onetime --no-daemonize
MAILTO=root
