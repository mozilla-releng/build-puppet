<%
if puppet_servers.respond_to?('shuffle')
    old_seed = rand()
    srand(ipaddress.sub('.', '').to_i)
    server = puppet_servers.shuffle[0]
    srand(old_seed)
else
    server = puppet_servers
end
-%>
<%= server -%>
