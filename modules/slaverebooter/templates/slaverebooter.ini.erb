[main]
verbose = false
slaveapi_server = <%=scope.lookupvar('config::slaverebooter_slaveapi')%>
workers = 12

[exclude]
# Hosts which match any key in this section will be skipped when processing
# slaves. Any values listed here will be ignored.
ec2 =
spot =
servo =
mavericks =
# Not enough jobs
panda =
fed =
